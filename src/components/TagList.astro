---
// expects props: tags (array)
const { tags } = Astro.props;
---
{tags && tags.length > 0 ? (
  <ul>
    {tags.map(tag => (
      <li><a href={`/tag/${tag}/`}>{tag}</a></li>
    ))}
  </ul>
) : null}
